import { version } from '../../package.json';

export const VERSION = version;

export const MINIMUM_LAUNCHER_VERSION = 0;

export const WEBSOCKET_BRIDGE_PROTOCOL_VERSION = 2;
export const SERVER_NET_BRIDGE_PROTOCOL_VERSION = 2;
